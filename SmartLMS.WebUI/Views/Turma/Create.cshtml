@model SmartLMS.WebUI.Models.TurmaViewModel

@{
    ViewBag.Title = "Inclusão de nova turma";
}

<div class="panel panel-info">
    <div class="panel-heading">
        Inclusão de nova turma
    </div>
    <div class="panel-body">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-horizontal">
                <h4>Dados da Turma</h4>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(model => model.Nome, htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.Nome, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Nome, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.IdsCursos, htmlAttributes: new { @class = "control-label col-md-2"})
                    <div class="col-md-10">
                        @Html.DropDownListFor(model => model.IdsCursos, ViewBag.Cursos as SelectList, new { @class="multiselect" , multiple = "multiple", data_multiselect_filter= false, data_multiselect_group = false })
                        @Html.ValidationMessageFor(model => model.IdsCursos, "", new { @class = "text-danger" })
                    </div>
                </div>
             
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <button type="submit" class="btn btn-success btn-sm">
                            <i class="fa fa-save"></i>
                        </button>
                        @Html.ActionLink("Voltar", "Index", null, new { @class = "btn btn-simple btn-sm btn-default" })
                    </div>
                </div>
            </div>
        }


    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script>
        $(function () {
            $("ul.multiselect-container").sortable({
                group: 'cursos',
                onDragStart: function ($item, container, _super) {
                    var offset = $item.offset(),
                        pointer = container.rootGroup.pointer;

                    adjustment = {
                        left: pointer.left - offset.left,
                        top: pointer.top - offset.top + $item[0].offsetTop - $(".multiselect-container").scrollTop()
                    };

                    _super($item, container);
                },
                onDrag: function ($item, position) {
                    $item.css({
                        left: position.left - adjustment.left,
                        top: position.top - adjustment.top 
                    });
                },
                onDrop: function ($item, container, _super) {
                    var options = $("select.multiselect").find("option");
                    var option = $("select.multiselect").find("option[value='" + $item.find("input").val() + "']");
                    var newIndex = $item.index();
                    var oldIndex = option.index();
                    $(options[oldIndex]).insertAfter($(options[newIndex]));
                    _super($item, container);
                }
            });
        });
        
    </script>
}
