@model SmartLMS.WebUI.Models.TalkToUsViewModel
<div class="modal fade" id="faleConosco" tabindex="-1" role="dialog" aria-labelledby="Fale Conosco" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
                    <i class="material-icons">clear</i>
                </button>
                <h4 class="modal-title">Talk to us</h4>
            </div>
            @using (Html.BeginForm("TalkToUs", "Home", null, FormMethod.Post, new { id = "talk-to-us-form" }))
            {
                @Html.AntiForgeryToken()
                <div class="modal-body">
                    <div class="input-group">
                        <span class="input-group-addon">
                            <i class="material-icons">email</i>
                        </span>
                        <div class="form-group label-floating">
                            <label class="control-label">Type your email</label>
                            @Html.TextBoxFor(x => x.Email, new { @class = "form-control", id = "talk-to-us-email-textbox", autocomplete = "email" })
                            @Html.ValidationMessageFor(x => x.Email)
                        </div>
                    </div>
                    <div class="input-group">
                        <span class="input-group-addon">
                            <i class="material-icons">person</i>
                        </span>
                        <div class="form-group label-floating">
                            <label class="control-label">Type your name</label>
                            @Html.TextBoxFor(x => x.Name, new { @class = "form-control", id = "talk-to-us-name-textbox", autocomplete = "name" })
                            @Html.ValidationMessageFor(x => x.Name)
                        </div>
                    </div>
                    <div class="input-group">
                        <span class="input-group-addon">
                            <i class="material-icons">message</i>
                        </span>
                        @Html.TextAreaFor(model => model.Message, new { @class = "form-control", placeholder = "Type your message", rows = "5", id= "talk-to-us-message-textbox" })
                        @Html.ValidationMessageFor(x => x.Message)
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="submit" id="talk-to-us-send-button" class="btn btn-primary">Send</button>
                    <button type="button" class="btn btn-danger btn-simple" data-dismiss="modal">Close</button>
                </div>
            }
        </div>
    </div>
</div>