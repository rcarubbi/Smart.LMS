@using SmartLMS.Domain.Entities
@using System.Linq
@model IEnumerable<SmartLMS.Domain.Entities.Delivery.Classroom>

@{
    ViewBag.Title = "Classrooms";

}

<div class="col-md-12">
    <div id="subject-index-container">
        <div class="card">
            <div class="content">
                <h3 class="text-primary index-title">Classrooms</h3>
                <div class="list-group">
                    @foreach (var classroom in Model)
                    {
                        <div class="list-group-item">
                            <div class="row-action-primary">
                                <i class="fa fa-graduation-cap"></i>
                            </div>
                            <div class="row-content">

                                <h4 class="list-group-item-heading">@classroom.Name</h4>
                                <div class="row">
                                    <div class="col-md-4">
                                        <p class="list-group-item-text">@classroom.Courses.Count() @(classroom.Courses.Count() > 1 ? Parameter.COURSE_PLURAL : Parameter.COURSE)</p>
                                        <p class="list-group-item-text">@classroom.DeliveryPlans.Count() Delivery Plan(s)</p>
                                        <p class="list-group-item-text">@classroom.DeliveryPlans.SelectMany(dp => dp.Students).Count() Student(s)</p>
                                    </div>
                                    <div class="col-md-4">
                                        <div href="javascript:void(0);" class="btn-group btn-group-sm" role="group" aria-label="..." style="margin: 0px;">
                                            <a class="btn btn-success btn-sm show-courses-button" data-id="@classroom.Id" data-name="@classroom.Name" data-toggle="tooltip" data-placement="top" data-container="body" data-original-title="Courses">
                                                <i class="material-icons">school</i>
                                            </a>
                                            <a href="javascript:void(0);" class="btn btn-warning btn-sm show-students-button" data-id="@classroom.Id " data-name="@classroom.Name" data-toggle="tooltip" data-placement="top" data-container="body" data-original-title="Students">
                                                <i class="material-icons">group</i>
                                            </a>
                                            <a href="@Url.Action("Plans", new { ClassroomId = classroom.Id })" class="btn btn-info btn-sm " data-toggle="tooltip" data-placement="top" data-container="body" data-original-title="Delivery plan">
                                                <i class="fa fa-calendar delivery-plan-button"></i>
                                            </a>
                                        </div>
                                    </div>

                                </div>
                            </div>
                        </div>

                        <div class="list-group-separator"></div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>


@Html.Partial("_StudentsListModal")
@Html.Partial("_CoursesListModal")

@section scripts {
    <script src="~/Scripts/classroomProxy.js"></script>
    <script>
        (function (proxy) {
            var $private = {}, $public = {};

            $(function () {
 
                $private.ClassroomStudentsTemplate = Handlebars.compile($("#classroom-students-template").html());
                $private.ClassroomCourseTemplate = Handlebars.compile($("#classroom-courses-template").html());

                $(document).on("click", ".show-courses-button", $private.loadCourses);
                $(document).on("click", ".show-students-button", $private.loadStudents);
 
            });


            $private.loadStudents = function (e) {
                e.preventDefault();

                var classroom = {
                    id: $(this).data("id"),
                    name: $(this).data("name")
                };
                $("#students-list").find(".modal-title").text("Students subscribed in the classroom " + classroom.name);

                proxy.listStudents(classroom.id).done($private.showStudents);

            };

            $private.showStudents = function (data) {
                $("#students-container").html($private.ClassroomStudentsTemplate(data));
                $("#students-list").modal();
            };

            $private.loadCourses = function (e) {
                e.preventDefault();

                var classroom = {
                    id: $(this).data("id"),
                    name: $(this).data("name")
                };
                $("#courses-list").find(".modal-title").text("Included courses in the classroom " + classroom.name);
                proxy.listCourses(classroom.id).done($private.showCourses);
            };

            $private.showCourses = function (data) {
                $("#courses-container").html($private.ClassroomCourseTemplate(data));
                $("#courses-list").modal();
            };

            

        }(SmartLMS.ClassroomProxy));
    </script>
}